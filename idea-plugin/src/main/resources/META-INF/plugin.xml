<!-- Plugin Configuration File: https://plugins.jetbrains.com/docs/intellij/plugin-configuration-file.html -->
<idea-plugin>
    <!-- Unique identifier of the plugin. It should be FQN, cannot be changed between the plugin versions. -->
    <id>dev.anvilcraft.resource.cuminum.idea</id>

    <!-- Public plugin name should be written in Title Case.
         Guidelines: https://plugins.jetbrains.com/docs/marketplace/best-practices-for-listing.html#plugin-name -->
    <name>Cuminum</name>

    <!-- A displayed Vendor name or Organization ID displayed on the Plugins Page. -->
    <vendor url="https://www.anvilcraft.dev">Anvil-Dev</vendor>

    <!-- Description of the plugin displayed on the Plugin Page and IDE Plugin Manager.
         Guidelines: https://plugins.jetbrains.com/docs/marketplace/best-practices-for-listing.html#plugin-description -->
    <description>
        A simple library for handling Codec, Provide intelligent completion suggestions
    </description>

    <!-- Product and plugin compatibility requirements.
         Read more: https://plugins.jetbrains.com/docs/intellij/plugin-compatibility.html -->
    <depends>com.intellij.java</depends>
    <resource-bundle>messages.MyMessageBundle</resource-bundle>
    <!-- Extensions defined by the plugin.
         Read more: https://plugins.jetbrains.com/docs/intellij/plugin-extension-points.html -->
    <extensions defaultExtensionNs="com.intellij">
        <toolWindow id="MyToolWindow" factoryClass="dev.anvilcraft.resource.cuminum.idea.MyToolWindowFactory"
                    icon="AllIcons.Toolwindows.ToolWindowPalette"/>

        <!-- PSI 增强提供者：向 @AutoCodec 和 @AutoStreamCodec 类注入生成的字段 -->
        <lang.psiAugmentProvider language="JAVA" implementationClass="dev.anvilcraft.resource.cuminum.idea.CodecPsiAugmentProvider"/>

        <!-- 代码补全提供者：为 Codec 字段提供智能补全 -->
        <completion.contributor language="JAVA" implementationClass="dev.anvilcraft.resource.cuminum.idea.CodecCompletionContributor"/>

        <!-- 导航处理器：支持跳转到生成的 Codec 字段 -->
        <gotoDeclarationHandler implementation="dev.anvilcraft.resource.cuminum.idea.CodecNavigationHandler"/>

        <!-- 文档提供者：为 Codec 字段提供快速文档 -->
        <lang.documentationProvider language="JAVA" implementationClass="dev.anvilcraft.resource.cuminum.idea.CodecDocumentationProvider"/>
    </extensions>
</idea-plugin>